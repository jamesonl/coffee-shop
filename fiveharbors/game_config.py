class gameConfig(object):
    """docstring for gameConfig."""

    def __init__(self, starting_budget, logging, player_name, player_email):
        super(gameConfig, self).__init__()
        self.starting_budget = 50000
        self.logging = False
        self.player_name = "Jameson"
        self.player_email = "jameson@email.com"
